export interface TransactionHistory {
  date: string;
  student: string;
  enrollment: string;
  department: string;
  batch: string;
  blockchainProof: string;
  certificateId: string;
  certificateUrl: string;
}
